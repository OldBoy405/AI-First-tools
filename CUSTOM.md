# CUSTOM.md — tools 跨仓定制与待实现台账

本文件记录 tools 依赖外部平台配合、但尚未形成端到端闭环的定制能力。`pending` 项不得在 Skill、Pipeline 或 README 中描述为已交付。

## 待实现

| ID | 状态 | 能力 | 当前边界 | 实现条件 | 来源 |
|---|---|---|---|---|---|
| CUSTOM-TODO-001 | pending | Multica 驳回原因自动注入回修节点 | Multica 已在 `approval_record.reject_reason` 保存原因，但 v1 grant 不包含或签名该字段；daemon 只投递 grant；当前没有 Pipeline Runner，因此 tools 本轮只验签 reject grant、执行合法状态回退并中止正向 Pipeline | 建立可信的驳回原因传输契约，并由 Pipeline Runner 将其作为 `review_feedback` 传给回修 Skill；Multica 负责人在环与持久化，Pipeline 负责传递和重放，Skill 消费反馈，`crctl` 不写入未签名原因 | `../AI First Platform/docs/analysis/tools-tca-001-004-optimization-plan.md#6-tca-003签名审批契约`；2026-08-10 grilling 共识 |
| CUSTOM-TODO-002 | pending | Pipeline Runner 分派签名审批决定 | tools Pipeline 模板已有 `human_approval → approve-*` 顺序，但当前没有 Runner，无法保证 reject grant 也会进入 `approve-*`；不得用 `onFail: skip` 代替，因为那会同时放过超时、投递失败等技术故障 | Runner 将签名 `approve` 与 `reject` 都视为已捕获决策并调用对应 `approve-*` Skill；只有节点技术失败执行 `onFail: abort`。`approve-*` 调用 `crctl approve --grant`，reject 验签回退后的结构化非零结果中止正向链；覆盖四个审批阶段的端到端测试。该能力是 CUSTOM-TODO-001 自动注入的前置条件 | `../AI First Platform/docs/analysis/tools-tca-001-004-optimization-plan.md#6-tca-003签名审批契约`；2026-08-10 grilling 共识 |
| CUSTOM-TODO-003 | pending | Multica 应用 `owners` outbox 更新 CR Owner 投影 | tools 本轮约定真实 Owner 变更在账本提交后产生 `event_kind=owners`；Multica 已接受并持久化该事件类型，但 `SyncService.apply()` 当前对 `owners` 不更新 `cr.owners`，不得把“事件已写出/接收”描述为“平台 Owner 已同步” | 在 Multica 为 `owners` 事件定义并校验 payload，按 workspace/CR/commit 幂等更新 `cr.owners` 与投影指针，发布 `cr.updated`；并扩展 Git/daemon 权威快照 reconcile Owner 投影，覆盖 outbox 缺失、乱序和重复事件 | `../AI First Platform/docs/analysis/tools-tca-001-004-optimization-plan.md#5-tca-002正式移交契约`；2026-08-10 grilling 共识 |
| CUSTOM-TODO-004 | pending | Multica 消费 `inbox` outbox 完成通知投递 | tools 本轮约定真实 Owner 变更同批写 backlog `owner-handover` 通知记录，并在提交后产生 `event_kind=inbox`；Multica 当前仅接受并持久化 `inbox`，没有通知投影或邮件/IM/站内信 handler，不得声称新 Owner 已收到通知 | 明确通知投递目标与幂等键，消费 `inbox` payload，形成可观测的站内信或外部触达结果；以 backlog `notify-log` 为漏事件 reconcile 来源，并覆盖重复投递、失败重试和死信处理 | `../AI First Platform/docs/analysis/tools-tca-001-004-optimization-plan.md#5-tca-002正式移交契约`；2026-08-10 grilling 共识 |
| CUSTOM-TODO-005 | pending | Multica 从 registration commit 恢复注册事件 | tools 本轮约定成功的 `[cr] register {CR-ID}: ...` 提交产生 `status + owners` outbox；outbox 非阻断且可能写入失败，但 Multica commit scanner 当前不识别 register 提交，daemon snapshot 又只对账状态且 backlog 已不含 status，因此注册事件缺失时不能完整自愈 | daemon commit scanner 识别稳定的 register 模板，从该提交读取 `cr.md`/backlog，重建 `(new) → drafting` 的 `status` 事件及完整 `owners` 事件；与本地 outbox 按 `(cr_id, commit_sha, event_kind)` 幂等去重，并覆盖 outbox 缺失与重复双通道测试 | `../AI First Platform/docs/analysis/tools-tca-001-004-optimization-plan.md#4-tca-001注册契约`；2026-08-10 grilling 共识 |
| CUSTOM-TODO-006 | pending | incomplete registration 跨进程续跑 | `cr-init` 三文件 CAS 成功后 CR-ID 已永久占用；本轮只要求同一次 `requirement-register` 执行在后续 commit/push/worktree 失败时返回 `REGISTRATION_INCOMPLETE` 并继续使用原 `cr_id`，当前没有 Runner 或恢复入口能在进程退出后从 `completed_steps` 自动续跑 | 定义持久化 registration checkpoint，由 Pipeline Runner 或专用恢复入口验证已完成 Git/worktree 事实后从失败步骤继续；必须复用原 CR-ID、保持幂等且不得重新调用 `cr-init`，覆盖 commit/push/第 N 个 worktree 失败后的跨进程恢复测试 | `../AI First Platform/docs/analysis/tools-tca-001-004-optimization-plan.md#4-tca-001注册契约`；2026-08-10 grilling 共识 |
| CUSTOM-TODO-007 | pending | per-repo publish lock 细粒度并发 | 本次 merge/workspace 优化采用按 registration key/CR-ID 的 operation lock，加 Installation Workspace 级短时 publish lock；共享账本与 knowledge-base trunk 发布会被串行，但 fetch、prepare 和确定性转换不占锁。当前不预先实现 per-repo 锁 | 仅当锁等待时间、队列长度或端到端事务耗时的实测数据证明跨进程发布吞吐不足时，将 publish lock 按 repo/ref 细分；必须保持共享 knowledge-base 账本与 Git index 的单写者约束，并补充死锁顺序及多仓并发故障测试 | `../AI First Platform/docs/analysis/tools-tca-005-009-015-016-merge-workspace-optimization-plan.md#84-锁粒度`；2026-08-10 grilling 共识 |
| CUSTOM-TODO-008 | pending | 核实并按证据引入通用事务框架 | 本轮只实现公共 journal envelope、lock/write-set/fault/recovery primitives，以及 register/workspace/merge/writeback/archive 五个显式函数；不建 Transaction class、phase engine、handler registry、adapter 或 plugin。各事务恢复语义不同，当前框架只会把差异藏进 callback | 仅在至少三个处理器出现相同的非平凡阶段控制、同一恢复缺陷需三处修复、出现真实第三方事务扩展，或事务数量/dispatch 维护成本有测量证据时再抽取。实施时优先在现有 `durable-tx.mjs` 从三个真实调用点提炼最小 phase runner，保留 op-specific payload/reconcile；逐个处理器迁移并复跑 crash/fault/remote observation tests。没有真实外部扩展前不得增加 plugin registry 或第三个事务模块 | `../AI First Platform/docs/analysis/tools-tca-005-009-015-016-merge-workspace-optimization-plan.md#72-公共-envelope与具体处理器`；2026-08-10 grilling 共识 |
| CUSTOM-TODO-009 | pending | release snapshot 协议切换后删除临时 upgrade-check | 本轮为硬升级边界新增只读 `crctl upgrade-check`，从 origin trunk/active repo refs 分类 safe/requiresReapproval/blocksUpgrade；它不修改 workspace，但只服务旧审批到 signed release snapshot 的一次切换，不应永久扩展 crctl API | 所有已知 Installation Workspace 均已运行新协议，且扫描确认不存在无 snapshot 的 code-approved/partial merge/merging/writing-back CR 后，删除 upgrade-check dispatch/help/实现/测试及本条；保留新协议运行时 `RELEASE_SNAPSHOT_MISSING` 门禁，不删除 release snapshot 本身 | `../AI First Platform/docs/analysis/tools-tca-005-009-015-016-merge-workspace-optimization-plan.md#135-release-snapshot-升级门禁`；2026-08-10 grilling 共识 |
| CUSTOM-TODO-010 | pending | 终态反馈事实受控写回 | `CONTEXT.md` 已定义以 `(CR-ID, spec-id)` 唯一标识的终态反馈事实，但当前 `feedback-writeback` 只有 prompt 契约，会直接手写 traceability/tech-notes 并发送与 canonical 语义冲突的 inbox；没有 `crctl` 原子写入、真实调用方或消费者。当前 Skill 必须退出 active 能力，且不得把领域模型描述为已交付 | 出现真实调用方与消费者后注册独立实施 CR；复用终态查询、durable write-set 与 Git authority，由 `crctl` 校验 `_history.yml` 唯一终态和 `writeback-spec-id`，原子写入 baseline `feedback[]` 与 history 输入摘要，覆盖同输入幂等、不同输入冲突及无 outbox 语义。不得预留占位命令、空字段或兼容分支 | `../AI First Platform/docs/analysis/tools-cr-lifecycle-minimal-optimization-spec.md#12-延期事项与启动条件`；2026-08-14 grilling 共识 |
| CUSTOM-TODO-011 | pending | 跨 CR `depends-on` 调度器 | 当前 5 条实施 CR 只需要推荐人工集成顺序；每条 CR 的状态、门禁、CAS、账本和失败恢复仍由现有 `crctl` 独立负责。本轮不建立跨 CR 依赖图、调度状态或第二套门禁事实源，避免把 Agent、Pipeline 与 crctl 职责重新混合 | 只有在出现可复现且频繁的并行 CR 顺序阻塞、人工无法稳定判断依赖、依赖关系可表达为确定性机器规则、并有跨团队/跨仓长期并行需求与真实消费者时，才注册独立实施 CR；优先复用现有 CR 状态/门禁/Git authority，不复制状态机，不让调度器直接写受控账本；必须覆盖依赖环、上游 reject/回修/取消、跨仓 ref 漂移、调度器中断恢复和手工旁路可见性测试 | `../AI First Platform/docs/analysis/tools-cr-lifecycle-minimal-optimization-spec.md#12-延期事项与启动条件`；2026-08-14 grilling 共识 |
